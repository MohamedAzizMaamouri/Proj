{% extends 'base.html.twig' %}

{% block title %}Commande confirmée - WatchStore{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="text-center mb-5">
                    <i class="fas fa-check-circle fa-4x text-success mb-3"></i>
                    <h1 class="display-4 text-success">Commande confirmée !</h1>
                    <p class="lead">Merci pour votre achat. Votre commande a été traitée avec succès.</p>
                </div>

                <div class="card">
                    <div class="card-header bg-success text-white">
                        <h4 class="mb-0">
                            <i class="fas fa-receipt me-2"></i>
                            Détails de la commande #{{ order.id }}
                        </h4>
                    </div>
                    <div class="card-body">
                        <div class="row mb-4">
                            <div class="col-md-6">
                                <h6>Informations client</h6>
                                <p class="mb-1"><strong>{{ order.user.firstName }} {{ order.user.lastName }}</strong></p>
                                <p class="mb-1">{{ order.user.email }}</p>
                                <p class="mb-0">Commande passée le {{ order.createdAt|date('d/m/Y à H:i') }}</p>
                            </div>
                            <div class="col-md-6">
                                <h6>Adresse de livraison</h6>
                                <p class="mb-0">{{ order.shippingAddress|nl2br }}</p>
                            </div>
                        </div>

                        <h6>Articles commandés</h6>
                        <div class="table-responsive">
                            <table class="table table-sm">
                                <thead class="table-light">
                                <tr>
                                    <th>Produit</th>
                                    <th>Prix unitaire</th>
                                    <th>Quantité</th>
                                    <th>Total</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for item in order.orderItems %}
                                    <tr>
                                        <td>
                                            <strong>{{ item.product.name }}</strong><br>
                                            <small class="text-muted">{{ item.product.brand }} - {{ item.product.model }}</small>
                                        </td>
                                        <td>{{ item.price|number_format(2, ',', ' ') }} €</td>
                                        <td>{{ item.quantity }}</td>
                                        <td><strong>{{ item.subTotal|number_format(2, ',', ' ') }} €</strong></td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                                <tfoot class="table-light">
                                <tr>
                                    <th colspan="3">Total de la commande</th>
                                    <th class="text-primary">{{ order.total|number_format(2, ',', ' ') }} €</th>
                                </tr>
                                </tfoot>
                            </table>
                        </div>

                        <div class="alert alert-info mt-4">
                            <h6><i class="fas fa-info-circle me-2"></i>Prochaines étapes</h6>
                            <ul class="mb-0">
                                <li>Vous recevrez un email de confirmation sous peu</li>
                                <li>Votre commande sera préparée dans les 24-48h</li>
                                <li>Vous recevrez un email avec le numéro de suivi</li>
                                <li>Livraison estimée : 3-5 jours ouvrés</li>
                            </ul>
                        </div>

                        <div class="text-center mt-4">
                            <a href="{{ path('app_home') }}" class="btn btn-primary me-2">
                                <i class="fas fa-home me-2"></i>Retour à l'accueil
                            </a>
                            <a href="{{ path('app_order_history') }}" class="btn btn-outline-primary">
                                <i class="fas fa-history me-2"></i>Mes commandes
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
